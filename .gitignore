node_modules
# Keep environment variables out of version control
.env
/src/generated/prisma
.harness/
web/.env.local
web/.env*.local

# Caderno de bordo local (guias em linguagem simples — não versionar)
/guias/